spring:
  profiles:
    active: dev


---
spring:
  profiles: dev
spring:
  application:
    name: springcloud-config-dev

---
spring:
  profiles: test
spring:
  application:
    name: springcloud-config-test